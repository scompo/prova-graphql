const expect = require('chai').expect;

describe('A dumb test', function() {
  describe('has not even a class', function() {
    it('should pass every time', function() {
      expect(true).to.equal(true);
    });
  });
});
